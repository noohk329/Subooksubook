<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/home"
        app:showAsAction="always"
        android:icon="@drawable/home_icon"
        android:iconTint="@drawable/item_color"
        android:enabled="true"
        android:checked="true"
        android:title="@string/home_name"
        />

    <item
        android:id="@+id/diary"
        app:showAsAction="always"
        android:enabled="true"
        android:checked="true"
        android:icon="@drawable/diary_icon"
        android:iconTint="@drawable/item_color"
        android:title="@string/diary_name"
        />

    <item
        android:id="@+id/statistics"
        app:showAsAction="always"
        android:enabled="true"
        android:checked="true"
        android:icon="@drawable/statis_icon"
        android:iconTint="@drawable/item_color"
        android:title="@string/statis_name"
        />

    <item
        android:id="@+id/setting"
        app:showAsAction="always"
        android:enabled="true"
        android:checked="true"
        android:icon="@drawable/setting_icon"
        android:iconTint="@drawable/item_color"
        android:title="@string/setting_name"
        />



</menu>